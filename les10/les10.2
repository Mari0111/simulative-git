files=$(find . -type f | grep -vE '(les10|les7|mydir)')
for file in $files; do
	c=$(grep -ow "$@" $file 2> /dev/null | wc -l)
	if [[ $c > 0 ]]; then
		result="$result\n$c $file"
	fi
done

echo -e "$result" | tail -n +2 | sort -rnk 1		
